import { prisma } from './src/lib/prisma'; async function test() { const user = await prisma.user.findFirst(); const weekStart = new Date(); weekStart.setDate(weekStart.getDate() - 7); weekStart.setHours(0,0,0,0); const sets = await prisma.setLog.findMany({ where: { isCompleted: true, workoutLog: { session: { userId: user.id, startTime: { gte: weekStart } } } }, include: { workoutLog: { include: { session: true } } } }); console.log('SETS', sets.length); } test();
